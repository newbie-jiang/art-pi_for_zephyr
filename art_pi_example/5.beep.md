# 5.beep

功能：蜂鸣器哔哔三声

## 编译

```
  west build -p always -b art_pi -d build 5.beep -- \
  -DCONFIG_BOOTLOADER_MCUBOOT=y \
  -DCONFIG_MCUBOOT_SIGNATURE_KEY_FILE="\"$HOME/zephyrproject/bootloader/mcuboot/root-rsa-2048.pem\""
```

